name: smooth_image_viewer_example
description: Example app for smooth_image_viewer.
publish_to: none

environment:
  sdk: ^3.11.3
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter
  smooth_image_viewer:
    path: ..

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
